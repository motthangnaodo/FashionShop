@model IEnumerable<FashionShopConnection.LoaiSanPham>
<li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown">Product Types</a>
    <div class="dropdown-menu">
        <div class="dropdown-inner">
            <ul class="list-unstyled">
                @foreach (var item in Model)
                {
                    <li><a href="@Url.Action("Index","LoaiSanPham",new { id=@item.MaLSP})">@item.MaLSP</a></li>
                }
            </ul>
        </div>
    </div>
</li>
